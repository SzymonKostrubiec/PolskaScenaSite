function setOptions(query, options) {
    if (!options.method) {
        options.method = "POST";
    }
    if (!(options.headers instanceof Headers)) {
        options.headers = new Headers(options.headers);
    }
    if (options.headers.get("Content-Type") === null) {
        options.headers.set("Content-Type", "application/json");
    }
    if (options.method !== "GET" && !options.body) {
        options.body = JSON.stringify({ query });
    }
    return options;
}
export default async function fetchQuery(url, query, options = {}) {
    const response = await fetch(url, setOptions(query, options));
    const body = (await response.json());
    if (body?.errors) {
        // oxlint-disable-next-line no-throw-literal
        throw { response, body };
    }
    return { response, body };
}
//# sourceMappingURL=fetchQuery.js.map