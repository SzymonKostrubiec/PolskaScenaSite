var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
import React from 'react';
import { ReactQueryDevtools } from '@tanstack/react-query-devtools';
import { useQueryClient } from '@tanstack/react-query';
import { Layout } from 'react-admin';
const DevtoolsLayout = (_a) => {
    var { children } = _a, props = __rest(_a, ["children"]);
    const client = useQueryClient();
    return (React.createElement(Layout, Object.assign({}, props),
        children,
        React.createElement(ReactQueryDevtools, { initialIsOpen: false, client: client })));
};
export default DevtoolsLayout;
//# sourceMappingURL=DevtoolsLayout.js.map