"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.Translate = void 0;
const react_1 = __importStar(require("react"));
const useTranslate_1 = require("./useTranslate.cjs");
const SPLIT_MARKER = '#@RA_CORE_INTERNAL_SPLIT@#';
const Translate = ({ i18nKey, options, children }) => {
    const translate = (0, useTranslate_1.useTranslate)();
    // Separate React element values from plain values
    const elementMap = {};
    const sanitizedOptions = {};
    let placeholderIndex = 0;
    if (options) {
        for (const [key, value] of Object.entries(options)) {
            if ((0, react_1.isValidElement)(value)) {
                const placeholder = `TRANSLATION_PLACEHOLDER_${placeholderIndex++}`;
                elementMap[placeholder] = value;
                sanitizedOptions[key] =
                    `${SPLIT_MARKER}${placeholder}${SPLIT_MARKER}`;
            }
            else {
                sanitizedOptions[key] = value;
            }
        }
    }
    const translateOptions = typeof children === 'string'
        ? { _: children, ...sanitizedOptions }
        : sanitizedOptions;
    const translatedMessage = translate(i18nKey, translateOptions);
    if (!translatedMessage) {
        return children;
    }
    // If no elements were extracted, return plain string
    if (placeholderIndex === 0) {
        return react_1.default.createElement(react_1.default.Fragment, null, translatedMessage);
    }
    // Split the translated string and replace placeholders with React elements
    const parts = translatedMessage.split(SPLIT_MARKER);
    return (react_1.default.createElement(react_1.default.Fragment, null, parts.map((part, index) => index % 2 === 1 ? (react_1.default.createElement(react_1.default.Fragment, { key: index }, elementMap[part])) : (react_1.default.createElement(react_1.default.Fragment, { key: index }, part)))));
};
exports.Translate = Translate;
//# sourceMappingURL=Translate.js.map